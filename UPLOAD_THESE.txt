CanTec Rewards — Admin cannot reject bills (clear notify)

WHAT CHANGED (admin/finance.html only for this fix)
- Admin no longer sees Approve / Reject buttons on pending claims
- Yellow notice: "Admin view only — Finance staff process claims"
- If reject/approve is still triggered as admin → clear alert (not silent fail)
- API error messages shown in the alert (e.g. "Only Finance staff can reject…")

Backend already returned 403 for admin reject/approve — UI was failing silently.

UPLOAD: Cloudflare Pages → Admin project → admin/finance.html
(Worker not required for this fix)

Also includes previous login fix files if you need a full admin refresh:
  admin/login.html, admin/index.html, admin/config.js
