FIX: Same phone was still accepting different mobile numbers

CAUSE
- Device hash mixed unstable hardware signals; identical phones / UA drift changed the hash between claims
- installId was not used as the primary binding key

FIX
- Fingerprint is now installId-primary (stable localStorage/sessionStorage id per browser)
- Block checks BOTH device_fingerprint_hash AND json_extract(device_raw_json, $.installId)
- Extra server guard on installId before accept

DEPLOY BOTH (required)
1) Worker:  src/fraud.ts, src/index.ts  (+ src/types.ts if not already deployed)
2) Claim portal: public/app.js

Test: submit once with mobile A, then same phone different mobile B → must show block banner.
Note: claims made BEFORE this deploy may not share the new installId link; retest with two NEW submits after deploy.
