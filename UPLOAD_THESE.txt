CanTec Rewards — Same phone + different contact number = block

ACCURACY
Yes — device blueprint is a practical way to catch one phone submitting under many mobiles.
We also fixed an unstable canvas fingerprint (was using Math.random every load) and added a
stable localStorage installId so the same browser stays linked reliably.

BEHAVIOUR
- Same device fingerprint + different mobile than prior claims → claim blocked (HTTP 403)
- Same device + same mobile (repeat claims) → allowed
- Customer sees a clear red banner on the claim form (not a silent fail)

UPLOAD
1) Worker (src/):
   - src/fraud.ts   (NEW in this package if missing on Worker)
   - src/types.ts
   - src/index.ts
2) Claim Portal Pages (public/):
   - public/app.js

Admin Pages — not required for this change.
